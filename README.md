Проект имитирует систему электронного пропуска, сверяет ID с репозиторием(список) и выводит информацию.

-Немного изменены классы для корректности тестов.

-Добавлено несколько тестов на Employee и EmployeeRepo для проверки корректности добавления сотрудника в список и параметризованный на отрицательные ID.

-Тесты с Mock добавлены для main, но не работают, при test в Maven выходит ошибка, возможно конфликт версий, так и не разобрался.

-Добавлен плагин checkstyle со встроенной конфигурацией на соответствие стандартам оформления.

-В Developer только один коммит, так как пропала ветка при подключении к гитхабу, остальные в main.

-Немного изменены классы для корректности тестов.

-Добавлено несколько тестов на Employee и EmployeeRepo для проверки корректности добавления сотрудника в список и параметризованный на отрицательные ID.

-Тесты с Mock добавлены для main, но не работают, при test в Maven выходит ошибка, возможно конфликт версий, так и не разобрался.

-Добавлен плагин checkstyle со встроенной конфигурацией на соответствие стандартам оформления.

-Добавлены параметризованные тесты для граничных значений

-Создан контекст приложения AppContext, вынесена логика создания объектов, создан AppContextTest с тестами (проверка, что объект не пустой и проверка, что возвращается именно переданная зависимость).

- Тесты с Mock по прежнему не работают, решил конфликты версий, но ошибка осталась в MainTest и AppTest.

- Тесты граничных значений реализованы в EmployeeRepoTest, создание объектов было только в Main, поэтому рефакторинг коснулся только его.

- Проведен рефакторинг в чистую архитектуру, добавлен SpringBoot. Классы распределены по domain, application и infrastructure, PassAplication вынесен в корень, т.к. Spring не видел вложенные папки изнутри. 
Наследников AccessType пришлось вынести в отдельные классы т. к. их не было видно без Public.
Создан интерфейс EmployeeRepository т. к. логика работы с репозиторием в классе EmployeeRepo. Новый репозиторий - интерфейс тот же.
Отдельно создан InMemoryEmployeeRepository т.к. подразумевается работа с бд и локальными файлами, для добавления которых не должны меняться Domain и application область.
Аналогично ConsoleOutputHandler и ConsoleInputHandler могут заменяться другими способами ввода и вывода. OutputPort может использоваться любым новым слассом вывода без изменений.
